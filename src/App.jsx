import React, { Suspense } from 'react'; import { OrbitControls, Environment } from '@react-three/drei'; import DarkMatter from './gallery/DARK_MATTER'; export default function App() { return ( <> <color attach='background' args={['#000']} /> <OrbitControls makeDefault /> <ambientLight intensity={0.5} /> <directionalLight position={[10, 10, 5]} intensity={1} /> <Suspense fallback={null}> <DarkMatter /> </Suspense> </> ) }
