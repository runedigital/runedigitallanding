import React, { useRef } from 'react'; import { useFrame } from '@react-three/fiber'; export default function App() { const ref = useRef(); useFrame((state, delta) => (ref.current.rotation.x = ref.current.rotation.y += delta)); return ( <> <ambientLight intensity={3} /> <directionalLight position={[10, 10, 5]} /> <mesh ref={ref} scale={1.5}> <boxGeometry /> <meshNormalMaterial /> </mesh> </> ) }
