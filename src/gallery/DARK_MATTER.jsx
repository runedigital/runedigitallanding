import React, { useRef } from 'react'; import { useFrame } from '@react-three/fiber'; import { MeshDistortMaterial, Sphere, Stars } from '@react-three/drei'; export default function DarkMatter(props) { const ref = useRef(); useFrame((state) => { const t = state.clock.getElapsedTime(); ref.current.distort = 0.4 + Math.sin(t) * 0.2; ref.current.rotation.x = t * 0.1; ref.current.rotation.y = t * 0.2; }); return ( <group {...props}> <Stars radius={100} depth={50} count={5000} factor={4} saturation={0} fade speed={1} /> <Sphere args={[1, 64, 64]} ref={ref} scale={2}> <MeshDistortMaterial color='#050505' emissive='#220033' roughness={0.1} metalness={1} distort={0.5} speed={2} /> </Sphere> <mesh scale={2.2}> <sphereGeometry args={[1, 32, 32]} /> <meshBasicMaterial color='#440055' wireframe transparent opacity={0.1} /> </mesh> </group> ); }
