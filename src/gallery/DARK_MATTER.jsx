import React, { useRef } from 'react'; import { useFrame } from '@react-three/fiber'; import { MeshDistortMaterial, Sphere, Stars, Float, Sparkles } from '@react-three/drei'; export default function DarkMatter(props) { const ref = useRef(); useFrame((state) => { const t = state.clock.getElapsedTime(); ref.current.distort = 0.4 + Math.sin(t) * 0.2; ref.current.rotation.x = t * 0.1; ref.current.rotation.y = t * 0.2; }); return ( <group {...props}> <Stars radius={100} depth={50} count={5000} factor={4} saturation={0} fade speed={1} /> <Float speed={2} rotationIntensity={1} floatIntensity={2}> <Sphere args={[1, 64, 64]} ref={ref} scale={2}> <MeshDistortMaterial color='#050505' emissive='#100020' roughness={0.1} metalness={1} distort={0.5} speed={2} /> </Sphere> <mesh scale={2.05}> <sphereGeometry args={[1, 64, 64]} /> <meshBasicMaterial color='#8000ff' wireframe transparent opacity={0.05} /> </mesh> <Sparkles count={200} scale={6} size={2} speed={0.4} opacity={0.5} color='#a020f0' /> </Float> </group> ); }
